---
import '../styles/global.css';
import { megaMenuData } from '../data/navigation';
const { title } = Astro.props;
---
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title} | Talkin Technologies</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
  </head>
  <body class="bg-slate-50 text-slate-900 font-['Outfit'] antialiased selection:bg-indigo-500 selection:text-white relative">

    <div class="fixed top-6 left-0 w-full z-50 px-4 lg:px-8">
      <nav class="max-w-[1400px] mx-auto bg-white/90 backdrop-blur-xl border border-white/50 shadow-2xl shadow-slate-200/50 rounded-2xl h-[72px] flex items-center justify-between px-4 lg:px-6 transition-all duration-300">
        
        <a href="/" class="flex items-center gap-3 group">
          <div class="w-9 h-9 bg-slate-900 rounded-lg flex items-center justify-center text-white font-bold text-lg group-hover:bg-blue-600 transition-colors">T</div>
          <span class="text-lg lg:text-xl font-bold text-slate-900 tracking-tight">Talkin<span class="text-blue-600">Tech</span></span>
        </a>

        <div class="hidden xl:flex items-center justify-center gap-1">
          {megaMenuData.map((menu) => (
            <div class="group relative px-4 py-3 cursor-pointer">
              <button class="text-[15px] font-semibold text-slate-600 group-hover:text-blue-600 transition-colors flex items-center gap-1.5">
                {menu.trigger}
                <svg class="w-3 h-3 text-slate-400 group-hover:text-blue-600 transition-transform group-hover:rotate-180" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" /></svg>
              </button>
              
              {menu.type === 'mega' && (
                <div class="absolute top-full left-1/2 -translate-x-1/2 pt-6 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 transform translate-y-2 group-hover:translate-y-0">
                  <div class="w-[900px] bg-white rounded-2xl shadow-2xl border border-slate-100 overflow-hidden ring-1 ring-slate-900/5 p-2">
                    <div class="flex">
                      <div class="flex-1 p-6 grid grid-cols-2 gap-8">
                        {menu.columns.map((col) => (
                          <div>
                            <h4 class="text-[11px] font-bold text-slate-400 uppercase tracking-widest mb-4">{col.heading}</h4>
                            <ul class="space-y-2">
                              {col.items.map((item) => (
                                <li>
                                  <a href={item.href} class="flex items-start gap-3 group/item p-2 hover:bg-slate-50 rounded-xl transition-colors">
                                    <div class="w-8 h-8 rounded-lg bg-blue-50 text-blue-600 flex items-center justify-center text-lg flex-shrink-0 group-hover/item:bg-blue-600 group-hover/item:text-white transition-colors">
                                      {item.icon}
                                    </div>
                                    <div>
                                      <div class="flex items-center gap-2">
                                        <span class="text-[14px] font-bold text-slate-700 group-hover/item:text-blue-700">{item.name}</span>
                                        {item.badge && <span class="text-[9px] bg-indigo-50 text-indigo-600 border border-indigo-100 px-1.5 py-0.5 rounded-full font-bold uppercase">{item.badge}</span>}
                                      </div>
                                      <p class="text-[12px] text-slate-400 leading-tight mt-0.5 line-clamp-1">{item.desc}</p>
                                    </div>
                                  </a>
                                </li>
                              ))}
                            </ul>
                          </div>
                        ))}
                      </div>
                      <div class="w-[260px] bg-slate-50 p-6 flex flex-col justify-end rounded-xl m-1 border border-slate-100">
                        <span class="text-[10px] font-bold uppercase tracking-widest text-indigo-500 mb-2">{menu.featured.title}</span>
                        <h3 class="text-lg font-bold text-slate-900 mb-2">{menu.featured.name}</h3>
                        <p class="text-xs text-slate-500 mb-4 leading-relaxed">{menu.featured.desc}</p>
                        <a href={menu.featured.link} class="text-center w-full bg-slate-900 text-white text-xs font-bold py-2.5 rounded-lg hover:bg-blue-600 transition-colors">View Details</a>
                      </div>
                    </div>
                  </div>
                </div>
              )}
              {menu.type === 'simple' && (
                <div class="absolute top-full left-1/2 -translate-x-1/2 pt-6 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 transform translate-y-2 group-hover:translate-y-0">
                  <div class="w-[240px] bg-white rounded-2xl shadow-2xl border border-slate-100 overflow-hidden ring-1 ring-slate-900/5 p-2">
                    <ul class="space-y-1">
                      {menu.items.map((item) => (
                        <li>
                          <a href={item.href} class="flex items-center gap-3 p-3 hover:bg-slate-50 rounded-xl transition-colors group/item">
                            <span class="text-lg group-hover/item:scale-110 transition-transform">{item.icon}</span>
                            <span class="text-[14px] font-bold text-slate-700 group-hover/item:text-blue-700">{item.name}</span>
                          </a>
                        </li>
                      ))}
                    </ul>
                  </div>
                </div>
              )}
            </div>
          ))}
        </div>

        <div class="flex items-center gap-3">
          <a href="/contact" class="hidden md:block bg-slate-900 hover:bg-blue-600 text-white text-[14px] font-bold px-6 py-2.5 rounded-xl shadow-lg shadow-slate-900/10 transition-all active:scale-95 whitespace-nowrap">
            Book Demo
          </a>
          <button onclick="toggleMobile()" class="xl:hidden p-2 text-slate-900 bg-slate-100 rounded-lg hover:bg-slate-200 transition-colors">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path></svg>
          </button>
        </div>
      </nav>
    </div>

    <div id="mobile-menu" class="fixed inset-0 bg-white/95 backdrop-blur-xl z-[100] hidden flex-col pt-28 px-6 overflow-y-auto transition-opacity duration-300">
      <button onclick="toggleMobile()" class="absolute top-8 right-8 p-3 bg-slate-100 rounded-full text-slate-900 hover:bg-slate-200">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
      </button>
      <div class="max-w-md mx-auto w-full space-y-3 pb-20">
        {megaMenuData.map((menu) => (
          <details class="group bg-slate-50 rounded-2xl border border-slate-100 overflow-hidden">
            <summary class="flex justify-between items-center p-5 cursor-pointer font-bold text-lg text-slate-800 select-none">
              {menu.trigger}
              <span class="text-blue-500 transition-transform group-open:rotate-180">▼</span>
            </summary>
            <div class="px-5 pb-5 space-y-6">
              {menu.type === 'mega' ? (
                menu.columns.map((col) => (
                  <div>
                    <h4 class="text-xs font-bold text-slate-400 uppercase tracking-widest mb-3">{col.heading}</h4>
                    <ul class="space-y-3">
                      {col.items.map((item) => (
                         <li><a href={item.href} class="flex items-center gap-3 py-1"><span class="text-xl">{item.icon}</span><span class="text-sm font-semibold text-slate-700">{item.name}</span></a></li>
                      ))}
                    </ul>
                  </div>
                ))
              ) : (
                <ul class="space-y-3">
                  {menu.items.map((item) => (
                     <li><a href={item.href} class="flex items-center gap-3 py-1"><span class="text-xl">{item.icon}</span><span class="text-sm font-semibold text-slate-700">{item.name}</span></a></li>
                  ))}
                </ul>
              )}
            </div>
          </details>
        ))}
        <a href="/contact" class="block w-full text-center bg-blue-600 text-white font-bold text-lg py-4 rounded-xl mt-8">Book a Demo</a>
      </div>
    </div>

    <slot />

    <footer class="bg-slate-950 text-white pt-24 pb-12 mt-auto border-t border-slate-900">
      <div class="max-w-[1400px] mx-auto px-6">
        
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-6 gap-12 mb-20">
          
          <div class="lg:col-span-2">
            <a href="/" class="flex items-center gap-3 mb-6">
              <div class="w-10 h-10 bg-blue-600 rounded-lg flex items-center justify-center text-white font-bold text-xl">T</div>
              <span class="text-2xl font-bold tracking-tight">Talkin<span class="text-blue-500">Tech</span></span>
            </a>
            <p class="text-slate-400 mb-8 leading-relaxed pr-8">
              Nairobi's premier software engineering house. We build the mission-critical infrastructure that powers East Africa's digital economy.
            </p>
            <div class="flex gap-4">
              <a href="#" class="w-10 h-10 rounded-full bg-white/5 flex items-center justify-center hover:bg-blue-600 transition-colors">𝕏</a>
              <a href="#" class="w-10 h-10 rounded-full bg-white/5 flex items-center justify-center hover:bg-blue-600 transition-colors">In</a>
              <a href="#" class="w-10 h-10 rounded-full bg-white/5 flex items-center justify-center hover:bg-blue-600 transition-colors">Fb</a>
            </div>
          </div>

          <div>
            <h4 class="font-bold text-white mb-6">Enterprise</h4>
            <ul class="space-y-4 text-slate-400 text-sm">
              <li><a href="/solutions/sacco-core" class="hover:text-blue-400 transition-colors">Sacco System</a></li>
              <li><a href="/solutions/smart-school" class="hover:text-blue-400 transition-colors">School ERP</a></li>
              <li><a href="/solutions/hospital" class="hover:text-blue-400 transition-colors">Hospital Mgmt</a></li>
              <li><a href="/solutions/hr-payroll" class="hover:text-blue-400 transition-colors">HR & Payroll</a></li>
            </ul>
          </div>

          <div>
            <h4 class="font-bold text-white mb-6">On-Demand</h4>
            <ul class="space-y-4 text-slate-400 text-sm">
              <li><a href="/solutions/taxi-app" class="hover:text-blue-400 transition-colors">Taxi/Uber App</a></li>
              <li><a href="/solutions/food-delivery" class="hover:text-blue-400 transition-colors">Food Delivery</a></li>
              <li><a href="/solutions/courier-app" class="hover:text-blue-400 transition-colors">Logistics</a></li>
              <li><a href="/solutions/courier-app" class="hover:text-blue-400 transition-colors">Courier System</a></li>
            </ul>
          </div>

         <div class="lg:col-span-2">
  <h4 class="font-bold text-white mb-6">Stay Updated</h4>
  <p class="text-slate-400 text-sm mb-4">Get the latest updates on Kenya's tech landscape and our new modules.</p>
  
  <div id="newsletter-success" class="hidden mb-4 p-3 bg-green-500/20 border border-green-500/50 rounded-lg text-green-400 text-xs font-bold">
    ✅ You're on the list!
  </div>

  <form id="newsletter-form" action="https://api.web3forms.com/submit" method="POST" class="flex gap-2 relative">
    
    <input type="hidden" name="access_key" value="5ccf7890-b960-4e71-9235-0a5915c33808">
    
    <input type="checkbox" name="botcheck" class="hidden" style="display: none;">
    
    <input type="hidden" name="subject" value="New Newsletter Subscriber">

    <input 
      type="email" 
      name="email" 
      required 
      placeholder="Enter your email" 
      class="bg-white/5 border border-white/10 rounded-lg px-4 py-3 text-sm text-white focus:outline-none focus:border-blue-500 w-full placeholder-slate-500" 
    />
    
    <button 
      type="submit" 
      id="newsletter-btn"
      class="bg-blue-600 px-6 py-3 rounded-lg text-sm font-bold text-white hover:bg-blue-500 transition-colors disabled:opacity-50 disabled:cursor-not-allowed"
    >
      Join
    </button>
  </form>

  <script is:inline>
    const newsletterForm = document.getElementById('newsletter-form');
    const newsletterBtn = document.getElementById('newsletter-btn');
    const newsletterSuccess = document.getElementById('newsletter-success');

    if (newsletterForm) {
      newsletterForm.addEventListener('submit', function(e) {
        e.preventDefault();
        
        // Loading State
        const originalText = newsletterBtn.innerText;
        newsletterBtn.innerText = "...";
        newsletterBtn.disabled = true;

        const formData = new FormData(newsletterForm);
        const object = Object.fromEntries(formData);
        const json = JSON.stringify(object);

        fetch('https://api.web3forms.com/submit', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'Accept': 'application/json'
            },
            body: json
        })
        .then(async (response) => {
            if (response.status == 200) {
                newsletterSuccess.classList.remove('hidden');
                newsletterForm.reset();
                // Hide success message after 5 seconds
                setTimeout(() => {
                  newsletterSuccess.classList.add('hidden');
                }, 5000);
            } else {
                alert("Something went wrong. Please try again.");
            }
        })
        .catch(error => {
            console.error(error);
            alert("Error sending message.");
        })
        .finally(() => {
            newsletterBtn.innerText = originalText;
            newsletterBtn.disabled = false;
        });
      });
    }
  </script>
</div>
        </div>

        <div class="border-t border-white/10 pt-8 flex flex-col md:flex-row justify-between items-center gap-4">
          <p class="text-slate-500 text-sm">© 2026 Talkin Technologies. All rights reserved.</p>
          <div class="flex gap-8 text-sm text-slate-500">
            <a href="/privacy" class="hover:text-white transition-colors">Privacy Policy</a>
            <a href="/terms" class="hover:text-white transition-colors">Terms of Service</a>
            <a href="/sla" class="hover:text-white transition-colors">SLA</a>
          </div>
        </div>
      </div>
    </footer>

    <script is:inline>
      function toggleMobile() {
        const m = document.getElementById('mobile-menu');
        m.classList.toggle('hidden');
        m.classList.toggle('flex');
        document.body.classList.toggle('overflow-hidden');
      }
    </script>
  </body>
</html>